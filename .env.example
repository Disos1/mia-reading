# Copy to .env.local (gitignored) and fill in.
# Only the PUBLISHABLE key ever goes here — it is frontend-safe by design.
# The sb_secret_* key must NEVER appear in this repo or any bundled file.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxx
